<div class = 'banner' style="background-image: url('<%= image_path('background.jpg') %>');" >
  <div class="container pt-5 form-position">
    <div class="row justify-content-center">
      <div class="col-12 col-md-6 col-lg-4" style="color: white">
            <h2 class="text-center mb-5">Find your <strong>Car</strong> </h2>
         <%= simple_form_for :search, url: listings_path do |f| %>
          <div class="form-group">
            <%= f.input :brand %>
            <%= f.input :production_year %>
            <%= f.button :submit ,class:"btn btn-primary btn-lg w-100"%>
          </div>
            <% end %>
      </div>
    </div>
</div>

c
