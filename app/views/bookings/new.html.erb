<body class="ct-dark">
  <div class="dark">
    <%= render 'components/navbar' %>
  </div>

  <div class= "container h-100">
    <div class="d-flex align-items-center justify-content-center h-100">
      <div>
         <h2 class="mt-5 pt-5 mb-5 text-light"> <%= current_user.name %>, complete your booking</h2>
         <div class ='row'>
            <div class="col-6">
                <%= render 'components/listing-card', listing: @listing %>
            </div>
            <div class="col-6 text-light">
              <div class="">
                <%= simple_form_for [@listing, @booking], method: :post  do |f| %>
                <%= f.input :days, collection: 1..30%>
                <% @booking.total_price = (10*@listing.price) %>
                <h4 id="total-price" class="mb-5"><strong>Total Price: €0</strong></h4>
                <br>
              </div>
                <div class="d-flex align-items-end">
                  <%= f.button :submit, 'BOOK!', class:"form-control btn btn-danger w-100 mt-5 rounded"%>
                </div>
              <% end %>
            </div>
          </div>
      </div>
    </div>
  </div>
</body>








